attribute vec4 a_vertexPosition;
// attribute vec2 a_circlePosition;
// varying vec2 v_circlePosition;

void main()
{
    gl_Position = a_vertexPosition;
    // v_circlePosition = a_circlePosition;
}
